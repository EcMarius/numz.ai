APP_NAME=Wave
APP_ENV=local
APP_KEY=base64:8dQ7xw/kM9EYMV4cUkzKgET8jF4P0M0TOmmqN05RN2w=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database
PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# For PRODUCTION: Use your production database
# For LOCAL: Override these in .env.local with MAMP settings
# ===========================================
DB_CONNECTION=sqlite
#DB_HOST=localhost
#DB_PORT=3306
#DB_DATABASE=database
#DB_USERNAME=root
#DB_PASSWORD=
#DB_COLLATION=utf8mb4_unicode_ci

BROADCAST_CONNECTION=log
CACHE_DRIVER=file
CACHE_STORE=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=9999
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
# For PRODUCTION with custom domain, set SESSION_DOMAIN to your domain with leading dot (e.g., .evenleads.com)
# This ensures cookies work properly across your entire domain
SESSION_SECURE_COOKIE=false
# For PRODUCTION with HTTPS, set SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax
# SESSION_SAME_SITE options: lax (recommended), strict, none

REDIS_HOST=localhost
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mail.softgala.com
MAIL_PORT=465
MAIL_USERNAME=evenleads@softgala.com
MAIL_PASSWORD="^q1jx,aa*9YE]^hf"
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS="evenleads@softgala.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

JWT_SECRET=SeCrEtKeY

WAVE_DEMO=false

# Add the Google Analytics Property ID (instructions can be found here: https://github.com/spatie/laravel-analytics)
ANALYTICS_PROPERTY_ID=ID_NUM

# ===========================================
# BILLING CREDENTIALS
# ===========================================
# These are default/example values
# For LOCAL: Override in .env.local with your test keys
# For PRODUCTION: Use production keys in .env
# ===========================================
BILLING_PROVIDER=stripe

PADDLE_VENDOR_ID=999
PADDLE_API_KEY=999
PADDLE_ENV=sandbox
PADDLE_PUBLIC_KEY=test_...
PADDLE_WEBHOOK_SECRET=pdl_...

STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ===========================================
# ENVIRONMENT FILE STRATEGY
# ===========================================
# .env.example     - Template (committed to git)
# .env             - Your actual environment file (NOT in git)
# .env.local       - Localhost overrides (NOT in git, only loaded when APP_ENV=local)
#
# Copy .env.example to .env and customize for your environment
# Create .env.local for localhost-specific settings (MAMP, ngrok, etc.)
# ===========================================
