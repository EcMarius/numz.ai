# EvenLeads Security Testing Configuration

# Target URL
BASE_URL=https://evenleads.com

# Traditional Login (if not using existing session)
TEST_EMAIL=your-test-email@example.com
TEST_PASSWORD=your-test-password

# ============================================================
# EXISTING SESSION MODE (NEW!)
# ============================================================
# Set ACCOUNT_EXISTING=true to use an existing browser session
# instead of programmatic login

ACCOUNT_EXISTING=false

# Option 1: Use Laravel Session Cookie (Recommended)
# Get this from browser Developer Tools → Application → Cookies
# LARAVEL_SESSION=your-session-cookie-value
# XSRF_TOKEN=your-xsrf-token  # Optional but recommended

# Option 2: Use API Token Directly
# API_TOKEN=your-api-bearer-token

# Option 3: Use Multiple Cookies as JSON
# SESSION_COOKIES={"laravel_session":"abc123","XSRF-TOKEN":"xyz789"}
